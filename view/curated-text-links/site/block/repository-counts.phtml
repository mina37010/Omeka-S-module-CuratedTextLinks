<div class="curated-text-repository-counts">
    <?php if (!empty($display['show_collections'])): ?>
    <div class="curated-text-repository-count">
        <span>コレクション</span>
        <strong><?php echo number_format((int) ($counts['collections'] ?? 0)); ?></strong>
    </div>
    <?php endif; ?>
    <?php if (!empty($display['show_items'])): ?>
    <div class="curated-text-repository-count">
        <span>アイテム</span>
        <strong><?php echo number_format((int) ($counts['items'] ?? 0)); ?></strong>
    </div>
    <?php endif; ?>
    <?php if (!empty($display['show_images'])): ?>
    <div class="curated-text-repository-count">
        <span>画像</span>
        <strong><?php echo number_format((int) ($counts['images'] ?? 0)); ?></strong>
    </div>
    <?php endif; ?>
</div>
